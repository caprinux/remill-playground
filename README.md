# remill-playground

```
Usage: ./build.sh <build-path>

  build-path: directory containing remill and llvm build
  Expected layout:
    <build-path>/remill-install/
    <build-path>/remill/dependencies/install/
```
